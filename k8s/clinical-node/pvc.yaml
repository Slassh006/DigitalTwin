apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clinical-data
  namespace: node-clinical
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: microk8s-hostpath
